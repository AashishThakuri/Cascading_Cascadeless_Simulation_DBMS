import React from 'react'
import ReactDOM from 'react-dom/client'
import TransactionSimulation from './react.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <TransactionSimulation />
    </React.StrictMode>,
)
